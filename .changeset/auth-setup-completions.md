---
"gws-rust": patch
---

Shell completion and `gwsr dev man` now cover `gwsr auth setup`'s own flags (`--project`, `--login`, `--dry-run`, `--non-interactive`) instead of a generic `ARGS` placeholder.
