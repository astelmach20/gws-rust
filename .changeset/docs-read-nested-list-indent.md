---
"gws-rust": patch
---

`docs +read --markdown` now indents nested list items to their parent's content column (three spaces under a numbered item, two under a bullet), so items nested under a numbered item stay nested when the Markdown is parsed again.
